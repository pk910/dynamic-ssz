# Codegen Backward Compatibility Tests

This directory contains snapshots of generated SSZ code from each released version of the library that included the `codegen/tests` harness. These snapshots ensure that code generated by older versions of `dynssz-gen` continues to compile and produce correct results against the current library.

## How it works

Each `codegen_v*.tar.gz` archive contains the full test package (type definitions, generated code, and tests) as produced by that version's `dynssz-gen`. The generated code is **not** regenerated -- the point is to verify the library remains backward compatible with previously generated code.

During CI, the archives are unpacked and the tests are run alongside the regular test suite via `go test ./...`.

## What the archives do and do not cover

An archive proves that code generated by that release still compiles against the
current library and still produces the same roots and encodings. That is the
delegation path: `DynSsz` finds the generated `Dynamic*` methods and calls them.

Archives up to v1.3.3 do **not** cover the current reflection engine against
their types. Their `testCodegenPayloadByReflection` predates `WithNoDelegation`,
so its "reflection" side also delegates to the generated methods, making it a
codegen-vs-codegen comparison. The live harness in `codegen/tests` passes
`WithNoDelegation` and does compare the two engines; archives cut from it after
that change inherit the fix.

The archives are deliberately left as their release cut them: regenerating one
would mean rebuilding that version's `dynssz-gen`, re-deriving its golden roots
and repacking, which would no longer be a snapshot of what that release shipped.

The generated code is never touched. A harness file may be corrected when it
asserted something the release's own code never delivered, and the correction
is recorded here:

- `v1.3.0`, `v1.3.1`, `codegen_test.go`, `TestCodegenCoverageTypes1`: the
  streaming encoders of these releases index past the end of a slice vector
  shorter than its declared length. The harness ran that type under specs that
  make it spec-bound, which now reaches the streaming encoder; it did not
  before, because a type whose only spec dependence was a limit was still
  served by its static methods. The case now runs the original short-vector
  payload under the default specs and a full-vector payload under the spec set.

## Local development

To unpack the archives for local testing:

```sh
make unpack-compat-tests
```

The unpacked `v*/` directories are gitignored. Only the `.tar.gz` archives are tracked.

## Adding a new version

New archives are created automatically by the release workflow (`prepare-release.sh`). It builds `dynssz-gen` from the release source, generates the test code, and packages everything into a new `codegen_v*.tar.gz`.
